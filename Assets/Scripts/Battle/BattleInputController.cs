using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleInputController : MonoBehaviour
{
    [SerializeField]
    private BattleManager battleManager;

    // Start is called before the first frame update
    void Start() { }

    private void OnAllyClick(BattleCharacter target) { }

    private void OnEnemyCLick(BattleCharacter target) { }

    private void OnAllyHover(BattleCharacter target) { }

    private void OnEnemyHover(BattleCharacter target) { }

    void Update() { }
}
